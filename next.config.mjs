/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,

    // basePath: '/poprigo.github.io',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;

